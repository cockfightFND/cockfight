// import { useIsRegistered } from "@initia/use-essentials"

// FIXME: update using correct method
function useIsNFTRegistered() {
  // const isRegistered = useIsRegistered()
  return (collectionAddress: string) => true
}

export default useIsNFTRegistered
